const config = {

    mountAppTo: '#app',
    requestPrefix: '/app',
    defaultRedirect: '/',

    plugins: {

        notification: {
            timeout: 9000
        }

    }

};

export default config
